interface:
  display_name: "Midjourney 使用助手"
  short_description: "分析需求并代你操作 Midjourney 生图与迭代"
  default_prompt: "使用 $midjourney-assistant 直接走正式运行路径承接我的需求，用统一编排器完成模式判断、prompt 生成与执行；不要向我播报内部脚本、命令、检查步骤、临时目录或权限探测。"

policy:
  allow_implicit_invocation: true
